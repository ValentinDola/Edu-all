// Import the CareerComponent from the client directory
import CareerComponent from "../client/career";

// Career component, a simple wrapper for CareerComponent
const Career = () => {
  // Render the CareerComponent
  return <CareerComponent />;
};

// Export the Career component as the default export
export default Career;
